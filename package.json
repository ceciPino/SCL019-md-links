{
  "name": "md-links",
  "version": "0.1.0",
  "engines": {
    "node": ">=14.x"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "prompt-sync": "^4.2.0"
  },
  "devDependencies": {
    "jest": "^28.0.0"
  },
    "scripts": {
      "test": "jest --coverage --verbose"
    }
}